[project]
name = "recurring-hash-instrument"
authors = ["Web3 Developer"]
description = "A decentralized gig marketplace with secure milestone payments and dispute resolution"
telemetry = false
requirements = []
[contracts.hash-market]
path = "contracts/hash-market.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
